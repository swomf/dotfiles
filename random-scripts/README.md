# random scripts.

Some self-contained scripts I keep in /usr/local/bin (excluding other software I keep in /usr/local).

## installation

```sh
# install via
make install
# uninstall via
make uninstall
```
